# Changelog

本文件记录本仓库的版本变更。版本号以 `src-tauri/tauri.conf.json` 为唯一来源。

## [0.6.1] - 2026-09-25

### 修复：股票资金记录的日期比委托日期早一天（东区）

「委托时间」只表达到"日"，界面把它落在**本地 00:00**；而服务层反算资金记录的日期时用的是 **UTC**，
于是东八区的 `2026-08-24 00:00`（= UTC `08-23 16:00`）被写成 `2026-08-23` —— 用户报的
"清仓选 8 月 24 日，资金变化列表显示 8 月 22 日"就是这层错位（成交时间那列先差一天，资金记录再被 UTC 拉早一天）。

- `tr_service::stock::unix_to_date` 改为按**宿主本地时区**取日期（`chrono::Local`），与界面
  `time::format_timestamp` 同口径；本地日期对"时间戳落在当天哪个时刻"不敏感，所以 00:00 与 12:00 都对。
- 连带修掉一个更严重的后果：被写成前一天的买卖资金记录会在**现金链**里被跳过
  （`recalculate_cash_chain` 取「(日期, 创建时间, ID) 最大一条」的余额，倒填的那条不是最大那条），
  **可用现金虚高**（用户真实数据上虚高了一整笔建仓的钱）。
- 旧数据自动订正：`stock::repair_legacy_trade_fund_dates` 在外壳打开工作空间时跑一次，
  只有"记录日期恰好比对应委托的本地日期早一天"才触发**重放**（日期与现金链一起重算）。
  条件是可证明的，当前版本写的数据永不满足，因此幂等、可反复执行。
  **升级提示**：装上本版后**第一次打开工作空间**就会自动订正历史日期与受影响的现金余额，不需要手工操作；
  订正只动能证明差一天的那些记录，其余原样保留。
- 回归：`cargo test -p tr-service`（本地日期口径 + 订正日期与现金链 + 幂等）、
  `fixtures/ui-stock.ps1` 第 1 步（日期选择器选**上个月的 24 号**（必然翻一次「上一月」）→ 选择器文案 /
  `trade_time` 的本地日期 / 资金记录 `record_date` / 账户页「资金变化记录 → 日期」列 四处一致）。

## [0.6.0] - 2026-09-25

### 股票操作可回滚

七种股票操作（支取 / 利息归本 / 追加本金 / 建仓 / 加仓 / 减仓 / 清仓）现在都留痕，**最多保留 10 条**，
入口在「股票 → 设置 → 操作记录」：一行两个按钮，「查看记录」弹窗列出这 10 条（动作 / 明细 / 时间），
「回滚」撤销**最新的一条**。

- 回滚前先**预演**（`preview_rollback`：只读，绝不落库），弹确认框把后果摆出来（要删哪条资金记录、
  要还原多少本金、要作废几笔成交），确认后才 `rollback_latest`。
- 逆操作只有两种形状：资金类（删掉对应资金记录；「追加本金」还要把 `principal` 还原并走
  `recalculate_cash_chain` 重算余额链）与委托类（删掉该委托及其成交）。**回滚本身不写日志**（否则会滚不动），
  「重置股票数据」会清空日志。
- 目标已不存在时（例如那笔成交已被手工删掉）**弹出该条并返回 `skipped`**，绝不把记录栈卡在一条再也滚不动
  的条目上。
- 新表 `tbl_billadm_stock_operation`（+ 账本索引），迁移 `20260925_stock_operation_log`（幂等、升级前
  `VACUUM INTO` 备份）；`tr-store` 的 `REQUIRED_COLUMNS` 与 `fixtures/schema/fresh.sql` 同步，
  `cargo xtask schema-diff` 与 `ledger` 的级联删除单测都已带上它。
- 回归：`fixtures/ui-stock.ps1` 末尾新增「操作记录与回滚」两段（做完「追加本金」→「查看记录」里能看到它 →
  「回滚 → 确认回滚」把本金与资金记录还原；这一步净效果为零，所以不影响后面的重置断言），服务层另有
  10 条单测（预演不落库、缺目标跳过、资金链重算等）。

### 统计页拆成「统计 / 明细」两栏，明细支持分页

- 工具栏左侧新增「统计 / 明细」页签（与「股票 → 设置」同款 `Tabs`）：**统计** = 结算统计 + 统计曲线，
  改成填满容器、不再整页滚动；**明细** = 逐笔结算明细表，**界面分页**（20 条/页，页脚「共 N 条」+ 页码），
  不再一次铺满整页。
- 顺带修掉一个既有缺陷：**统计页的缓存不会失效** —— 持仓 → 清仓 → 切到统计，刚清的那一笔不出现。
  缓存现在按「账本 + 筛选条件」为键，切换时按当前键重新校验；同内容不重复写信号（省掉一次无谓重建）。

### 图表纵轴：刻度取大整数，跨零时 0 落在正中；面积填充改以 0 轴为基线

- 纵轴刻度不再由 charts-rs「按数量级把步长往上取整」决定（用户报的 `0 / 9,500 / 19,000 / 28,500 / 38,000`
  现在是 `0 / 10,000 / 20,000 / 30,000 / 40,000`）：步长只从 **1 / 2 / 5 × 10^k** 里挑、最多 5 段，
  范围写进 `axis_min/axis_max/axis_split_number`。⚠ 自定义上界必须**严格**大于数据最大值，
  否则它会静默退回自己那套阶梯（表现是"范围白设了"）。
- **数据跨零时纵轴取上下对称的 `±half` 且段数取偶数**，于是 0 正好落在正中那条刻度上 —— 涨与跌从同一条
  基准线读，而不是各自从图的上下边缘起算。
- 面积填充的**基线从绘图区底边挪到 0 轴**：charts-rs 的 `StraightLineFill` 把 `bottom` 写死成绘图区底边
  （`bottom: axis_height`），数据既有正又有负时，负的那一段会被填成"从折线一路铺到图底"的大色块。
  它没暴露这个开关，所以在生成的 SVG 上定点改写收尾点（`anchor_fill_at_zero`，0 轴 y 由
  `get_offset_height` 的映射反推；只在范围可信、几何合理且路径是单段时才改写，否则原样返回）。
- 新增 `fixtures/chart-tests.ps1`：`tr-ui` 是 wasm-only crate（`lib.rs` 顶部
  `#![cfg(target_arch = "wasm32")]`，`cargo test -p tr-ui` 跑 **0 个**测试），该脚本把被测函数的**原文**
  抽出来配桩在 native 上真跑 —— 7 条断言，其中「填充基线」是用 **charts-rs 真实输出的 SVG** 做逐字节
  定点断言（新增/改动这些纯函数时请一并跑它）。
- 已知未修：charts-rs 的 `{t}`（千分位）**对负数不生效**（`thousands_format_float` 开头
  `if value < 1000.0` 把负数全挡了），所以同一根轴上会同时出现 `50,000` 与 `-50000`。

### 日记栏头

- 栏头去掉下方那条分隔线；三个按钮改成**透明无边框**（只在 hover 时有底色）；按钮整体右移，
  最左补上「日记列表」文字。

## [0.5.1] - 2026-09-24

### 股票资金变化记录翻页不生效

- 账户页的资金记录点「上一页 / 下一页 / 页码」时，页码高亮跟着走，**表格还是原来那几行**。根因是分页控件只写页码信号（`Pagination` 不负责取数），而资金记录只在「切换账本」和「提交资金操作」两处发起查询 —— 没有谁依赖页码去查数据。
- 修法：在 `account_view` 里加一个依赖「当前账本 + 页码」的 `Effect` 统一查资金记录（账本换了回第 1 页，页码变了按新页码查，服务端回写页码不会重复查），提交资金操作后走同一条路径。
- 回归落在 `fixtures/ui-stock.ps1`：11 条记录时第 1 页 10 行 →「下一页」1 行且日期集合与第 1 页不同 →「上一页」回到第 1 页同一批数据。

### 新增「利息归本」

- 账户工具栏在「支取」与「追加本金」**中间**新增「利息归本」（金额 + 发生日期，与另两个入口共用同一套弹窗）。
- 口径：**只进可用现金，本金不变** —— 本金仍是"自己投入的累计本金"，利息单独累计。落库为新的资金事件 `interest_principal`（金额为正、带现金余额），照常进资金链，所以支取上限也随之变大。
- 账户页指标区新增「利息归本」一项（累计额），可用现金 = 本金 + 累计利息归本 + 已实现盈亏 − 累计支取 − 持仓成本。
- 统计页的「总资产曲线」把利息归本算进去（最大回撤与账户页现金对得上），但最大回撤率的**分母仍是投入本金**。
- 回归：`fixtures/ui-stock.ps1` 走真实界面记一笔（按钮顺序、弹窗字段「利息金额」、落库事件与金额、本金不变、可用现金 +利息、指标区显示的两个金额），服务层另有本金/资金链/统计曲线的单测。

## [0.4.1] - 2026-09-23

### 统计曲线一直在闪

- 股票 → 统计的「统计曲线」**持续闪烁**：`statistics_view` 是普通函数，父级每次重建都重新调用它、并把里面的 Effect 重新创建一遍；而那个 Effect 用**自己的 `prev` 参数**判断"账本有没有换"，新 Effect 的 `prev` 每次都从 `None` 开始，于是每次重建都被判成"账本换了"，完整重跑初始化（清缓存 → 重新拉标签 → 写 `tags`）。`tags` 又被上层视图**同步读着**（`Select.options` 要的是 `Vec`，调用方只能 `.get()`），于是"写 `tags` → 上层重建 → 再重跑初始化"**自激循环**，实测每秒重渲染 30+ 次，每轮都重建整块图表 SVG、charts-rs 的**入场动画不断重播** —— 看起来就是曲线在闪（闪的是动画起点那一段）。
- 修法：① "已初始化账本"的判据放**模块级**（`STATS_LEDGER`），视图重建不再等于账本切换；② 标签选项也放模块级（`STATS_TAGS`），切子功能再回来下拉不空；③ 标签下拉的选项读 `tags` 改在它**自己的动态块**里，上层不再把它记成依赖；④ `tags` 同值不写（`RwSignal::set` 同值也会通知）；⑤ 真发生重建时把模块级缓存同步回本次信号，否则飞行中的请求结果会落进已销毁的信号、页面停在空态。
- 实测：修复前计数器 333 且每秒 +30，修复后 `view=1`、其余个位数，三次读数跨 3 秒完全不变。

### 图表图例的中文系列名互相重叠

- 图例里相邻两项叠在一起（`餐饮总支出` 与 `餐饮支出 - 商场` 首尾重叠），系列名越长越明显。根因是 charts-rs 用**它自己内置的 Roboto** 量文字宽度来排版图例，而 SVG 里的文字按页面字体（JetBrains Mono + 中文回退）渲染 —— 中文回退字更宽，"量出来 40px、画出来 60px"。它只能注册 TTF/OTF，而仓库里的字体是 woff2（fontdue 解析不了），量不准这件事没法从它那侧修。
- 修法：图例**改由界面自绘 HTML**（`flex-wrap: wrap`），换行与间距全部交给浏览器按真实字体度量排；过长的系列名走省略号，不会把整行撑破。

### 只有一个数据点时图表 tooltip 不显示

- `read_points` 把"每个序列至少要有两个 `<circle>`"当成有效判据，而只有一个类目时每个序列就只有一个数据点、只有一个圆 —— 整条序列被丢掉 → 几何为空 → tooltip 与竖参考线都不渲染。改为"非空即可"。

### 标题栏的功能标题改为居中小字

- 页面标题原来是**左上角的大号 display**（`记账`），与侧栏当前项、子功能图标条重复表达"你在哪一页"。现在它是标题栏正中的**小字**，文案 `Transactions-<页面名>`（如 `Transactions-记账`），caption 12px / 弱化色。
- 居中靠**左右等宽的内边距**（右上角那三个窗口按钮是绝对定位浮层，两侧各留出它的宽度），因此与标题自身宽度无关；窄窗口里长标题走省略号，不会把窗口三键顶开。

### 日记页日期行贴住工具栏

- 日记页右栏第一行是标题行（`2026年9月23日 星期三` + 心情/字数），而它只有下内边距：直接贴住工具栏底边、自己下方却空出 16px，"上间距像丢了"、内容顶在那条带的上沿。改为上下对称，日期行落在自身容器的垂直中线上。
- 根因不是意外：`6ee7bb7` 把各页内容容器的顶部内边距统一成 0（为了内容与工具栏顶对齐）。这一栏的第一行是**标题行**而不是内容，所以在这里按需要补回来。

### 更新下载的状态跨页面恢复

- 更新下载是**外壳侧的单例任务**（一次只有一笔），界面切换页面不会中断它。但原来界面只靠进度事件更新本地信号，切走再切回来时进度与「取消下载」就不见了，看着像"下载丢了"。
- 外壳新增 `update_download_status` 命令（无参数）：报告**是否有下载在跑**、**是否已下载好**、最近一次进度与速度；并把"正在下载"做成单例（重复触发直接回 `already_downloading`，不会起第二笔）。
- 界面再次进入「关于软件」时先问一次这个状态并据此恢复：下载中 → 显示进度与「取消下载」；已下载 → 显示「下载完成 / 安装并退出」；两者都没有 → 回到「立即更新」。首次进入仍然自动检查一次更新。
- 下载完成后若 `%TEMP%` 里的安装包被系统清理，状态不会再谎报"已下载"（否则点安装会拿到空文件）。
- 新增端到端护栏 `fixtures/ui-update-restore.ps1`：下载进行中切走再切回，断言仍是下载态（或已完成），并验证取消后回到可更新状态。

## [0.4.0] - 2026-09-22

### 股票统计页卡在「正在加载」

- 股票 → 统计页会**反复重建视图**（父级每次重渲染都重新调用 `statistics_view`，函数体里的信号与 Effect 跟着重建），而"账本变了就拉数据"的 Effect 每轮都从空状态起步 → 每轮发一次 `stock_statistics`。实测**几秒内上千次请求**、页面永远停在「正在加载」。
- 修法：统计的**数据、加载中标记与请求去重状态全部放到模块级**（`STATS_DATA` / `STATS_LOADING` / `STATS_DEDUPE`，线程局部），按「账本 + 筛选」去重；切账本时三者一起清。
  - 三者都必须放模块级：放组件信号里等于每轮重建后从空开始 —— 只做请求去重挡不住（请求数降到 1，页面却仍空白，因为回来的数据写进了**上一份已经被丢掉的信号**）。
  - 请求**在飞行中**也要挡：否则视图重建时会在飞行中又把 `loading` 置 true，页面就卡在「正在加载」——这正是本缺陷的表现。
  - 请求失败时会清掉去重键，保证下一次筛选（或重新进入本页）能再试一次。
- 统计工具栏去掉「刷新」按钮（页面用的数据在筛选变化时会自动重取，按钮既多余、又正好暴露了上面那个重建问题）。
- 顺带修好「最近 N 笔」下拉：它的绑定值原来写成 `value=RwSignal::new(...)`（把信号建在 props 里），视图每次重渲染都会新建一个、值弹回初始项，选不动。

### 图表提示跟随鼠标

- 折线图的 tooltip 原来**钉在图表顶部**（`top: 0`，只按命中的类目横向定位），现在**跟着鼠标走**：贴着指针显示，靠近画布左右边时自动收在边界内（浮层完整可见），上方放不下时翻到指针下方。

### 日记导入导出只支持 txt

- 导入只认 `YYYY-MM-DD.txt`，导出也只写 `<日期>.txt`（原来是 `.txt` 与 `.md` 都收，导出写 `.md`）。日记正文是纯文本、界面不做 Markdown 渲染，两个方向现在对称：导出的文件可以直接再导入。
- 目录里的 `.md` 等其它扩展名会被**跳过**（不报错、不入库）；界面上「未找到符合格式的日记文件」的提示与两张卡片的说明都改成只说 `.txt`。

### 一次委托多笔成交时的费用口径

- **佣金**仍然按**整笔委托**收一次（最低佣金也只收一次）；**印花税与过户费**改成**逐笔成交**各收一次、各笔自己四舍五入到分再相加 —— 券商本来就是按成交笔收费的。
- 这会改掉一分钱：两笔成交各 1 手的卖出（`36.61×100` = 3661.00、`36.67×100` = 3667.00，沪市），过户费从"按总额取整"的 `0.07` 变成"逐笔取整再相加"的 `0.08`，**预计到手 7319.27 → 7319.26**（印花税两笔各 1.83、共 3.66 不变；佣金 5.00 不变）。
- 界面上的「预估费用」与落库的费用同一份算法，不会再出现"弹窗显示 0.07、存进去 0.08"这类不一致；每笔成交分到的费用之和仍然恒等于委托级合计。

### 其它界面调整

- 事件卡片的删除二次确认从**气泡**改成**弹窗**：标题写成「删除事件「事件名」？」，确认按钮是危险样式，正文说明"删除后无法恢复，该事件的图片也会一起删掉"。删除失败时弹窗留着，可以直接再点一次。

### 功能开关

- 「应用设置」多一个分栏「功能开关」：记账 / 股票 / 事件 / 日记各一行，右侧一个开关，默认全开。关掉之后**侧边栏就不再显示**那一项（关掉的正好是当前页时，自动挪到还开着的第一个功能；全关了就回到设置页）。
- 开关改动**立即落盘**，不需要保存按钮，落在 `~/.transactions.json` 的 `features: { accounting, stock, keyEvent, diary }`。老配置里没有这个键 = 全开，升级后界面与之前完全一致。
- 「应用设置」本身**不可关闭**，也就没进这个列表：关掉它就没有界面能再把它打开。

### 股票页改成子功能图标条

- 股票页原来的顶部页签（账户 / 持仓 / 成交记录 / 交易统计）换成左侧图标条，与记账页同一套版心骨架。子功能五项：**账户 / 持仓 / 记录 / 统计 / 设置**，原来的「成交记录」改名「记录」、「交易统计」改名「统计」。
- 各子功能的主操作挪到工具栏：账户是「支取 / 追加本金」，持仓是「建仓」，统计是筛选组（全部 / 按时间 / 最近 N 笔 + 标签）。「建仓」原来在左侧持仓面板的底栏，位置跟着工具栏走，空态提示也从"先点下方"改成"先点上方"。（统计后来去掉了「刷新」按钮，见上。）
- 「应用设置 → 股票」整块迁到股票页的「设置」子功能：交易费用设置、交易标签、重置股票数据都在那里。「保存」进工具栏，标签的增删仍是即存即生效。费用设置与交易标签都是**按账本**存的，跟着股票事务走比留在应用配置里更顺。应用设置的页签因此由 4 个变 3 个（通用设置 / 日记配置 / 关于软件）。
- 功能页骨架的四个区域（侧栏 / 顶栏 / 内容 / 底栏）里，顶栏也变成可选，与侧栏、底栏一致：不需要工具栏的子功能不再留一条空栏。
- 「建仓」保持在左侧持仓面板的底栏（挪进工具栏的那版手感不对，已还原）。子功能图标条改用语义更贴的图标：账户=用户、持仓=金币、「记录」=历史（原来的「同步」线条在图标条上会画变形）。

### 弹窗宽度收成三档

- 弹窗宽度不再逐处写像素，改为三档：`Small` 400 / `Medium` 520 / `Large` 640（`ModalSize`，对应 `.ui-modal__content--sm|md|lg`）。此前 24 处弹窗用了 11 个不同宽度（360…800），同类弹窗宽窄不一。
- 「新增记录 / 编辑记录」原来宽到 800，单列表单被拉得很空，现在收到 `Large`（640）；「筛选条件」「添加曲线」同样是 640。确认类弹窗（删除账本 / 日记 / 分类 / 标签、追加本金、支取、重置股票数据等）收到 400。
- 不给档位的弹窗仍按 `Medium`（520），与改动前的默认值一致，其余弹窗外观不变。

## [0.3.0] - 2026-09-21

这版有两处会碰到用户数据。日记改成按账本隔离；既有工作空间在首次打开时自动升级，升级前先备份一份。
其余是新增代理设置、一次功能页骨架重做、三处功能改名，以及若干界面修复。

### 日记关联账本

- 日记从工作空间级改成账本级。一天一篇的唯一性由 `date` 变成 `(ledger_id, date)`，页面、导入、导出、删除都只作用于当前账本。切账本即切日记，同一天在两个账本可以各存一篇，互不覆盖。
- 导入落到当前账本，导出也只导当前账本。把两个账本导到同一个目录会按日期互相覆盖（文件名仍是 `YYYY-MM-DD.md`，方便再导入）。
- 没有选中账本时，日记页显示「未选择账本」引导，设置页的导入/导出按钮置灰。
- 删账本会连它的日记一起删掉，和事件、股票走同一套级联清理。
- 切账本时如果还有没落库的草稿，先写回草稿所属的旧账本，再切。不丢字，也不会串到新账本里。

### 工作空间自动迁移

- 新增 `tr-store` 的迁移引擎（`crates/tr-store/src/migrations.rs`）。打开既有工作空间时，按 `tbl_billadm_schema_migration` 登记表逐条应用待升级项，动手之前先备份成 `<workspace>/transactions.db.pre-migration-<时间戳>.bak`（`VACUUM INTO` 的单文件快照，含 WAL 内容）。备份失败就不升级，不留没退路的局面。
- 同一个工作空间只保留最近一份备份。新备份成功之后才删上一份，所以备份失败时旧的那份还在。只清理自己命名规则下的 `.bak`，工作空间里别的文件不动。
- 每条迁移在一个事务里完成，执行和写登记行一起提交，失败整体回滚，库保持原样。迁移写成幂等形式，重复打开不会重复改。
- 本次新增的迁移是 `20260920_diary_ledger_scope`：日记表加 `ledger_id`，旧的全局唯一索引换成 `(ledger_id, date)` 复合唯一索引；升级前的日记全部归到创建时间最早的账本（多个账本没法拆，只能整体归一个；一个账本都没有时留空值，界面上看不到，但不会报错）。
- `cargo xtask migrate <workspace-dir>` 不开界面就能升级，并打印迁移 id 与备份路径。`cargo xtask validate` 仍然只读，只是会报告有哪些待应用的迁移。
- 有一句文案变了：工作空间比当前格式更早、又没有可用升级路径时，错误提示不再说"本版本不会自动改写既有数据库"，改成建议换一个工作目录，或者用旧版本升级。

### 代理设置

- 「应用设置 → 通用设置」多了一行「代理」，三档：`不使用`、`自动探测`（默认）、`手动 http://host:port`。设置存在 `~/.transactions.json` 的 `proxy: { mode, url }`，老配置没有这个键就按 `auto` 处理。界面上直接显示当前走不走代理，探到就给出地址和来源，旁边的「检测」按钮可以随时再看一次。
- 自动探测的顺序：环境变量 `ALL_PROXY` / `HTTPS_PROXY` / `HTTP_PROXY`，然后是 Windows「Internet 选项」里的静态代理（先读 `HKCU`，再退到 `HKLM`），都没有就直连。系统里只配了 PAC 脚本的话，界面会说明本版本不解析，然后按直连走。每次请求都重新探测，改了系统代理不用重启。
- 只支持 HTTP 代理。`socks*://` 与 `https://` 会被拒绝并说明原因。HTTPS 目标（比如 GitHub）走同一个代理的 CONNECT 隧道。地址可以带 `user:pass@` 做 Basic 认证，`host:port` 会自动补上 `http://`。
- 行情查询（`qt.gtimg.cn`）和更新检查/下载（GitHub）读同一份设置，不会出现更新走代理、行情不走的半生效。
- 这是一处行为变更。以前两个客户端只认进程环境变量里的代理，那是 ureq 的默认行为，Windows 系统代理完全没读。现在默认 `auto` 跟随系统代理；「不使用」是彻底直连，环境变量代理也不用了。
- 回归脚本 `fixtures/ui-proxy.ps1` 起一个本机假 HTTP 代理当判据：手动代理下，行情与更新检查必须出现在它的请求日志里（行情还会断言界面显示的是假代理返回的名称）；切到「不使用」后一条都收不到。

### 命名调整

- 顶级功能「关键事件」改名「事件」：侧栏、页面标题，以及记账页的行内按钮和弹窗标题（「关联关键事件」改成「关联事件」），相关提示也改成「查询事件失败」。只动界面文案，IPC 命令名（`key_event_*`）、表名（`tbl_billadm_key_event*`）、列名（`key_event_date`）、资产目录（`data/assets/key_events`）、路由标识和领域术语都没碰。
- 顶级功能「股票交易」改名「股票」：侧栏、页面标题、应用设置里的分栏名，以及重置相关的三句文案（弹窗标题「重置股票数据」、提示「股票数据已重置」「重置股票数据失败」）。同样只动界面文案，命令名、表名、路由标识和"股票交易记录"这类领域术语保持原样。`fixtures/` 里按名字定位页面的脚本跟着改了。

### 信息架构

- 「消费记录」改名「记账」，并成为唯一带子功能的顶级功能，左侧图标条切换记录 / 标签 / 模板。
- 「分类标签」不再是顶级页面，变成记账的标签子功能；「消费模板」从设置页搬出来，变成记账的模板子功能。设置页因此只剩四个分栏：通用 / 日记 / 股票 / 关于。
- 子功能共用标题栏（标题固定「记账」）。切换子功能会重建视图，和切页一样，不留隐形的旧状态。
- 「数据分析」并入「记账」，成为第 2 个子功能并改名「分析」。侧栏顶级功能从 6 个减到 5 个（记账 / 股票 / 事件 / 日记 / 应用设置），图标条顺序由 `SubFunction::ALL` 决定，现在是记录 / 分析 / 标签 / 模板。它只提供工具栏和内容区，版心、标题栏、图标条依旧由记账页渲染，路由标识 `/da_view` 随之取消。
- 删掉零引用的占位页模块 `pages/placeholders.rs` 和它专属的 CSS，`Page::planned_commands` 一并移除。
- `fixtures/` 跟着同步：`ui-smoke`、`ui-shots`、`dev-shot` 的页面表改成 5 个顶级功能加 4 个子功能（`分析` 的标志性控件是「新增图表」），`ui-crud` 的图表用例改走记账页的「分析」子功能；README、PRODUCT、AGENTS 里的功能名与计数也更新了。

### 界面骨架

- 新增通用组件 `FeaturePage`。标题栏以下整块区域由它铺满，不再是卡片，去掉了外边距、边框、圆角和裁切。结构固定成 `子功能图标条（可选）+ 工具栏 + 内容区 + 底栏（可选）`：底栏只占主列，图标条在它左边一路通到底。
- 底栏高度和侧栏底部「应用设置」那条带共用同一个令牌（44px），两栏底边齐平。

### 界面修复

- 悬浮提示被压成一列窄条。「应用设置 → 股票」里过户费、印花税、交易费用说明的气泡只设了 `max-width`，而绝对定位的包含块是那个十几像素宽的 ⓘ 触发器，可用宽度就是它，长文案于是被压成窄条贴着窗口右缘，还带出滚动条。现在气泡先按文案铺开（`width: max-content`），再用上限收窄并受视口约束，带换行的多行说明按原样折行。触发器在版心右缘的提示改成右对齐向左铺开（`.ui-tooltip--end`），不再越出窗口。
- 「应用设置 → 关于软件」的内容不再垂直居中。页签面板当年从 `.st-pane` 改名成 `.page-pane` 时漏改了这个面板的类名，它退化成普通块级容器，`justify-content` 失效，`margin: auto` 也居中不了，内容停在顶部。类名补回来就好了。
- 侧栏导航的悬浮提示曾经显示 `/accounting_view`、`/da_view` 这类路由名。`nav_button` 把 `title` 写成了 `page.route()`，而 `route()` 是页面的稳定标识，只用于调试。先改成跟按钮文字同源的 `page.label()`，后来直接把这个 `title` 去掉了：按钮里本来就有可见文字（`.nav-btn-text`），再挂一个同名提示只是把旁边的字说第二遍，还多出一块盖住界面的浮层。无障碍名保留 `aria-label`，读屏照样能念出功能名。
- 设置行列表的最后一行补上了分隔线。`.st-list > .st-card:last-child` 原本有一条 `border-bottom: none`，理由是"下面就是版心底边"，但看起来像列表被截断，和上面几行不一致。现在每一行都有下边线，通用设置、日记配置、股票三个分栏一致。
- 「关于软件」去掉了「构建类型：开发版 / 正式版」那一行，页面上只留应用名、版本、更新检查、GitHub 链接和版权行。界面不再消费 `app_info("isDev")`（后端能力保留），`fixtures/ui-about.ps1` 改成反向断言这一行不再出现。
- 「关于软件」的 GitHub 链接补了图标，新增 24 格实心 `Icon::GitHub`，链接改成图标加文字一行。

### 日记页

- 编辑区的保存状态如实反映三态：输入或切心情后是「编辑中」，防抖保存时是「保存中…」，成功是「已保存」；换日期时状态重新起算。保存期间又改了内容，会补排一次保存，改动不会丢。
- 去掉「跳转日期」按钮，日期选择器选到哪天就跳哪天。

### 首启动

- 工作空间未指定时，「新建或打开工作空间」是一个关不掉的强制选择屏：没有 ×，没有取消，点遮罩不关。窗口关闭请求由外壳拒绝，逃生门是托盘的「关闭程序」。
- 这个屏只渲染自己，不再连侧栏一起塞进 600×560 的窗口里。再次启动应用图标时聚焦它，而不是新建主窗口。
- 修掉一个真实缺陷：初始化窗口选完目录后，主窗口是个空壳。WebView2 控制器从未建起来，界面永远不发 `config_get`，初始化窗口也不销毁。根因是在 WebView2 的 `WebMessageReceived` 回调里直接建新 webview，现在把切换推迟到回调之外。

### 其它

- 打开文件夹对话框时，只在路径真实存在时才拿它当起点，否则用用户主目录，不再弹「位置不可用」。
- fixtures 新增共享助手 `Invoke-SubFunction`（按最左边的同名 Button 点子功能），`ui-smoke`、`ui-shots`、`ui-crud`、`ui-drag`、`dev-shot` 随之覆盖三个子功能。

## [0.2.1] - 2026-09-20

**全仓去重与死代码清理**（117 个文件，+1690 / −3840，净删约 2150 行），外加两处护栏修复。
**行为不变**：数据格式、IPC 字段名与用户可见文案、界面渲染结果、配置键名全部未动，
单测数量与既有断言逐条保留。

### 清理

- **界面 `tr-ui`**：删掉 12 个零引用的 `pub` 项（`ipc::call_batch`、`api::tr::batch_create`、
  `api::stock::principal_set`、`format::money_axis_text`、`time::ym_to_seconds`、
  `AppStores::statistic`、`error_handler` 的三个空转 async 入口、`transactions::apply_result`、
  两处"注释理由是假的" `#[allow(dead_code)]` 函数等），以及 `base.css` 里 17 个零引用
  `.u-*` 工具类与 `@mixin` 清单（191 行）。
  - 费用预估改走 `tr_domain::fee::compute_order_fee`，股票代码校验与沪市判定改走
    `tr_domain::fee::{is_valid_stock_code, is_shanghai_code}` —— 此前界面自己抄了一份，
    属于"前后端各写一份、靠人工同步"的破口；界面仍保留 `amount <= 0` 守卫与饱和求和。
  - `YYYY-MM-DD` 解析与「闭区间 Unix 秒」换算收敛到 `crate::time`（消费记录页与数据分析页共用一份）。
  - 24 处类名拼接 → `ui::with_class`；弹窗/抽屉关闭按钮、浮层遮罩、日历翻页按钮、
    7 处页头、两处复盘编辑块抽成共享实现；`ui.css` / `app.css` 两处整段重复的声明合并。
- **内核 `tr-store` / `tr-domain` / `tr-ipc`**：category 与 tag 两个**逐字节相同**的分组统计
  合成 `dao::count_grouped_by`（表名/列名仍是常量、值仍是占位符）；删 9 个只有测试在调的
  `delete_by_ledger_id`（级联删除的活路径在服务层单事务里）与 `ApiError::new`、`TAG_POLICY_NOT`；
  `create_fund_record` 里写了两遍的 INSERT 合一；10 份测试夹具合成 `test_workspace`；
  tr-ipc 的 `require_ledger_id` / 日记参数校验 / `QueryNumber` 解析收敛为公共函数（文案逐字不变）。
- **服务层与外壳**：删 3 个零调用服务函数；行情解析的 first/all 两套扫描合一；
  日记日期校验复用 `parse_strict_date`（chrono），去掉手写公历实现；
  `stock` 的三对「workspace 版 / 事务内 `_in` 版」委托化；百分比、成本结转、标签判断、
  图表预设等抽公共实现；9 份测试夹具与 2 处 round 播种夹具合一；
  外壳的 `FileSaveResponse::failed`、`pick_path`、`theme_of` 收敛；删掉 `src-tauri` 两个
  未使用依赖（`thiserror`、`tr-service`）；`xtask` 的只读打开抽成一处。
- **护栏脚本 `fixtures`**：20 个端到端脚本此前**各自抄了一份** UIA 辅助函数
  （`Assert-True` 15 份、`Invoke-Element` 12 份、`Save-Screenshot` 11 份、C# 鼠标类 18 份…）。
  新增 `fixtures/lib/TrUia.ps1` 作为唯一底座（装配加载、C# `TrUia`、24 个共享函数），
  18 个脚本改为 dot-source：**18 个脚本合计 7162 → 5566 行**。
  语义不同的变体（更强的失效守卫、不同的默认超时/文案）按"宁可少抽"保留在各自脚本里。

### 修复

- `fixtures/contract-audit.ps1`：界面侧请求结构体表改为**跨 `crates/tr-ui/src/api/*.rs` 合并**
  （`api/mod.rs` 优先），共享结构体重新纳入逐字段比对 —— 比较数 **57 → 66**，
  "跳过（界面直接传 tr_domain 共享类型）"的原因说明恢复准确。
- `fixtures/lib/TrUia.ps1` 的 `Assert-True` 在找不到调用方脚本的 `$failures` 时**直接抛错**
  （原来会静默不计数 = 假绿）；`Read-Table` / `Start-App` 改为显式传参，
  避免模块作用域看不到脚本变量、让 cargo 在错误的工作目录里跑。
- `fixtures/ui-about.ps1` / 共享模块：本机**别的目录下**装着同款应用在跑时，
  单实例插件会顶掉脚本启动；现在会给出明确报错而不是等满超时。

### 验证

- `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings` 全绿；
  `cargo test` 单测数量不变（tr-domain 64 / tr-store 39 / tr-ipc 15 / tr-service 126 / 外壳 35），
  既有断言一条未删。
- `cargo xtask schema-diff` 通过：**数据库结构一字未动**。
- 端到端护栏与 **0.2.0 产物上的基线逐条对比**：同一批脚本、同样的断言数、无新增失败。
  （基线里本就有 9 个脚本为红，是 0.2.0 产物在本机环境/界面改版下的既有问题，
  与本次清理无关，逐条差异见 0.2.1 的 Release 说明。）

## [0.2.0] - 2026-09-20

图表引擎换代 + 界面布局与组件全面细化。**数据格式与配置键名均无变化**，
既有的 0.1.0 工作空间可直接打开（schema 未动）。

### 变更

- **图表**：折线图引擎由 `plotters` 换成 `charts-rs`（`default-features = false`，直出 SVG，
  `THEME_LIGHT` / `THEME_DARK` 跟随主题）；折线改直线段、数据点实心、图例与 Y 轴刻度列对齐、
  单序列面积填充；画布尺寸随容器自适应；游标 / tooltip / 参考线改由 DOM 覆盖层绘制。
- **股票**：修通四个分栏的高度链，「资金变化记录」分页行贴到卡片底部；持仓页左栏通栏、
  「建仓」贴底，卡片三态（软灰卡面 / 纸面悬停 / 强调色选中）；成交记录页详情改纸面卡、
  轮次详情标题独占一行；间距收敛为「区域 24 / 卡内 16 / 内容 12」三级节奏。
- **日记**：移除 Markdown 与预览、去掉底栏，工具栏承载保存状态与删除，编辑框上下留白一致。
- **关键事件**：卡片与关联交易卡改纸面 + 发丝描边，选中用强调色底。
- **消费记录 / 设置 / 分类标签 / 数据分析**：分页、抽屉、日期选择器、空态等细节统一；
  新增 `time_range_picker` 组件，移除未使用的 `float_button`。

### 修复

- **发布：0.2.0 的安装包资产曾经是 0.1.0 的安装包**（用户下载安装后仍是 0.1.0 的界面，
  两个 release 的资产字节数与 `sha256` 完全相同）。原因：`cargo tauri build` 不清 NSIS 产物目录，
  `build/build.ps1` 用 `Get-ChildItem *-setup.exe | Select-Object -First 1` 取**字典序第一个**，
  于是上一版遗留的 `Transactions_0.1.0_x64-setup.exe` 被改名成 `Transactions-x64-v0.2.0.exe` 上传。
  现在构建前先清掉陈旧安装包，只认 `Transactions_{版本}_x64-setup.exe`，
  并断言它是**本轮构建**产出的文件，安装包与便携版都要落盘成功，否则直接失败退出。

### 验证

- 新增 `fixtures/ui-about.ps1`：断言打包产物自报的版本号（「设置 → 关于软件」）与
  `tauri.conf.json` 一致，并覆盖关于页其余固定内容与更新检查终态。
- 新增 `fixtures/dev-hot.ps1`（trunk 与外壳的热更新桥）与 `fixtures/dev-shot.ps1`（按页截图）。

## [0.1.0] - 2026-09-19

首个版本：**Tauri 2 外壳 + Leptos(WASM) 界面 + rusqlite 内核**的纯 Rust 桌面记账应用。

### 新增

- **桌面外壳**（Tauri 2）：无边框自绘标题栏、单实例、托盘菜单与"显示主窗口/退出"、
  三种关闭行为（退出 / 最小化到托盘 / 每次询问）、窗口大小与位置按逻辑像素记忆、
  自定义 `trasset://` 资产协议（含路径穿越校验）、深/浅色双主题。
- **界面**（Leptos 0.8 CSR → WASM，仓库内无 Node/npm）：消费记录（记一笔/模板/编辑/删除/同步到其他账本/
  筛选/排序/分页/统计条）、数据分析图表（自绘 SVG）、股票交易全链路（建仓/加仓/减仓/清仓/成交记录/
  轮次归档/费用设置/盈亏统计/行情查询）、关键事件（日期唯一、配色、Markdown、关联消费、图片附件）、
  日记（Markdown 预览与编辑、导入/导出、心情、字数）、设置（通用/消费模板/日记/股票/关于软件）。
- **内核**（Rust）：`tr-domain`（纯领域层，金额恒为整数分、费用分摊）/ `tr-store`（rusqlite + 连接池）/
  `tr-service`（业务规则，不依赖 tauri）/ `tr-ipc`（唯一依赖 tauri 的命令面）。
  界面与内核之间只走 Tauri IPC，**没有本机 HTTP 内核、不监听端口、无子进程**。
- **数据**：每个工作空间一个独立 SQLite 数据库，建库以 `fixtures/schema/fresh.sql` 为基线；
  已存在的工作空间**只做只读校验**，绝不执行 DDL/DML 改结构；`~/.transactions.json` 位置与键名稳定，
  读写保留未知键。**不含任何数据迁移代码**：更早格式的工作空间会被明确拒绝。
- **更新检查**（自研，不用 `tauri-plugin-updater`）：查本仓库 GitHub Release，
  下载安装包并按 `sha256` 校验后安装；支持进度/取消/已下载复用。
- **验证护栏**：`cargo xtask schema-diff`（建库结构与基线逐条一致）、
  17 个端到端脚本（启动形态、关闭行为、7 页渲染+写入闭环、股票全生命周期、日记编辑与导入导出、
  关键事件、拖拽排序、窗口几何、图片上传、像素与主题、设计令牌与跨 crate 契约审计等），
  清单见 `AGENTS.md` 的「常用命令」。

### 说明

- 界面按 `DESIGN.md` 的设计令牌实现；图表与 Markdown 都是界面层自绘 / 纯 Rust 渲染，不引入任何 JS 库。
- 已知偏差与取舍（行情与更新检查的 HTTP 客户端不读系统代理、资金记录 `created_at` 的严格递增规则等）
  记在 `AGENTS.md`。

[0.4.1]: https://github.com/ddd-online/Transactions-Rust/releases/tag/v0.4.1
[0.4.0]: https://github.com/ddd-online/Transactions-Rust/releases/tag/v0.4.0
[0.3.0]: https://github.com/ddd-online/Transactions-Rust/releases/tag/v0.3.0
[0.2.1]: https://github.com/ddd-online/Transactions-Rust/releases/tag/v0.2.1
[0.2.0]: https://github.com/ddd-online/Transactions-Rust/releases/tag/v0.2.0
[0.1.0]: https://github.com/ddd-online/Transactions-Rust/releases/tag/v0.1.0
