# Changelog

本文件记录**本仓库**（Transactions 的纯 Rust 重写版）的版本变更。
它自己的版本号与参考实现（原 Electron + Vue 3 + Go/Gin 版，最后版本 v0.27.0）**各自独立**：
两边共用同一份工作空间数据格式（最新 schema），但发布节奏与版本号无关。

## [0.1.0] - 2026-09-19

首个公开版本：把 Transactions 从 Electron + Vue 3 + Go/Gin 整体重写为
**Tauri 2 外壳 + Leptos(WASM) 界面 + rusqlite 内核**的纯 Rust 栈，功能与数据格式保持等价。

### 新增

- **桌面外壳**（Tauri 2）：无边框自绘标题栏、单实例、托盘菜单与"显示主窗口/退出"、
  三种关闭行为（退出 / 最小化到托盘 / 每次询问）、窗口大小与位置按逻辑像素记忆、
  自定义 `trasset://` 资产协议（复刻原实现的路径穿越校验）、深/浅色双主题。
- **界面**（Leptos 0.8 CSR → WASM，仓库内无 Node/npm）：消费记录（记一笔/模板/编辑/删除/同步到其他账本/
  筛选/排序/分页/统计条）、数据分析图表（自绘 SVG）、股票交易全链路（建仓/加仓/减仓/清仓/成交记录/
  轮次归档/费用设置/盈亏统计/行情查询）、关键事件（日期唯一、配色、Markdown、关联消费、图片附件）、
  日记（Markdown 预览与编辑、导入/导出、心情、字数）、设置（通用/消费模板/日记/股票/关于软件）。
- **内核**（Rust）：`tr-domain`（纯领域层，金额恒为整数分、费用分摊）/ `tr-store`（rusqlite + 连接池）/
  `tr-service`（业务规则，不依赖 tauri）/ `tr-ipc`（唯一依赖 tauri 的命令面）。
  界面与内核之间只走 Tauri IPC，**不再有本机 HTTP 内核、不监听端口、无子进程**。
- **数据**：沿用参考实现 0.27 的最新 schema，**可以直接打开 Electron 版 0.27 的工作空间**
  （已存在时只做只读校验，绝不执行 DDL/DML 改结构）；`~/.transactions.json` 位置与键名不变，
  读写保留未知键。**不含任何数据迁移代码**：更早版本的工作空间会被明确拒绝。
- **更新检查**（自研，不用 `tauri-plugin-updater`）：查本仓库 GitHub Release，
  下载安装包并按 `sha256` 校验后安装；支持进度/取消/已下载复用。
- **验证护栏**：`cargo xtask schema-diff`（建库结构与基线逐条一致）、
  `cargo xtask parity`（与参考实现同输入双写、18 张表全列逐字段的黄金对比）、
  17 个端到端脚本（启动形态、关闭行为、7 页渲染+写入闭环、股票全生命周期、日记编辑与导入导出、
  关键事件、拖拽排序、窗口几何、图片上传、像素与主题、设计令牌与跨 crate 契约审计等）。

### 说明

- 界面是**重新实现**的，不是逐像素复刻：组件与图标按 `DESIGN.md` 的设计令牌重建，
  与原 Vue 组件清单的对应关系见 `docs/UI-KIT.md`。
- 已知偏差与取舍（行情/更新的 HTTP 客户端不读系统代理、资金记录 `created_at` 与 Go 侧秒级精度的差异、
  与参考实现对不上的个别文案等）逐条记在 `docs/ACCEPTANCE.md` 与 `AGENTS.md`。
- 许可证为 Apache-2.0，与参考实现一致。

[0.1.0]: https://github.com/ddd-online/Transactions-Rust/releases/tag/v0.1.0
